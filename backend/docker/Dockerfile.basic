FROM python:3.10-slim

RUN apt-get update && apt-get install -y \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

RUN pip install --no-cache-dir jupyterlab nbconvert nbformat ipykernel numpy pandas scikit-learn matplotlib

WORKDIR /app
CMD ["python", "run_notebook.py"]
